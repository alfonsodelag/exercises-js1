// Declare your function first
const sumNumbers = (number1, number2) => {
    return number1+number2;
}

const sum = sumNumbers(13, 124)
// Call the function and assign to a variable `sum`

console.log(sum);
